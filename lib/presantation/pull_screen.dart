import 'package:flutter/material.dart';

class pull_screen extends StatelessWidget {
  const pull_screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Pull to Refresh')));
  }
}